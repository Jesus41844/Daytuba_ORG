# --- Base de datos (PostgreSQL) ---
# Desarrollo: docker compose -f docker-compose.dev.yml up -d
DATABASE_URL=postgres://utp:utp@localhost:5433/utp_tasks

# --- Archivos (PDFs adjuntos) ---
UPLOADS_DIR=./uploads

# --- Moodle sync ---
MOODLE_ENCRYPTION_KEY=
